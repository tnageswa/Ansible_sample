---
 - name: Ansible archive Example
   user: root
   hosts: all
   tasks:
    - name: Compress the file and remove
      become: yes
      archive:
        path: /home/db2inst1/sqllib/db2dump/db2diag.log
        dest: /home/db2inst1/sqllib/db2dump/db2diag.log.gz
        remove: yes

