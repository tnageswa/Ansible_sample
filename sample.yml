---
 - name: Ansible archive Example
   user: anstadm
   hosts: all
   tasks:
    - name: Compress the file and remove
      become: yes
      archive:
        path: /tmp/db2diag.log
        dest: /tmp/db2diag.log.gz
        remove: yes

