---
 - name: update SUSE patches
   hosts: all
   become: yes
   become_user: anstadm
   any_errors_fatal: false
   tasks:
   - name: Create a file
     file:
      dest: /etc/nag.txt
      state: touch
      owner: root
      group: root
      mode: '0644'
