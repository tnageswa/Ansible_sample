---
- hosts: all
  remote_user: ansadmin
  become_method: sudo
  vars: 
   ansible_python_interpreter: /usr/bin/python
  tasks:
  - name: Create a file
    file: path=/home/ansadmin/nag.txt state=touch
  
