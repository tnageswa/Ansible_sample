---
 - name: Upgrade patches on Host(s)
   hosts: 9.190.27.146
   user: anstadm
   become: true
   any_errors_fatal: false
   tasks: 
     - name: Test for available disk space
       setup: filter=ansible_mounts
       
     - name: Ensure that free space on the tested volume is greater than 60%
       assert:
         that: 
          - mount.size_available > mount.size_total|float * 0.6
         msg: Disk space has reached 40% threshold
       vars:
         mount: "{{ ansible_mounts | selectattr('mount','equalto',item.mount) | list | first }}"
       with_items:
         - "{{ ansible_mounts }}"
